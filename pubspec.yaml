name: seeme_grow
description: A child growth timeline app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.6

  # IDs
  uuid: ^4.3.3

  # Local storage
  shared_preferences: ^2.2.2

  # File system & images
  path_provider: ^2.1.2
  image_picker: ^1.0.7
  file_selector: ^1.0.3

  # Share
  share_plus: ^7.2.1

  # Legacy / preview (سيتم تقييمه لاحقًا للإزالة)
  flutter_screen_recording: ^2.0.0

  # 🔹 Cloud
  supabase_flutter: ^2.5.6

  # 🔹 Network awareness
  connectivity_plus: ^6.0.5

  # 🔹 State management / architecture
  riverpod: ^2.5.1
  flutter_riverpod: ^2.5.1

  # 🔐 Secure configuration
  flutter_dotenv: ^5.1.0

  # 🔔 Local notifications
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - .env

flutter_launcher_icons:
  android: true
  ios: true
  macos:
    generate: true
    image_path: "assets/icon/app_icon.png"
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  